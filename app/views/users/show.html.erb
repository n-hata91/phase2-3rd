<h2>Users</h2>
<%= render 'partial/userinfo', book: @book %>

<div>
  <tr>
    <th></th>
    <th>Title</th>
    <th>Opinion</th>
    <th></th>
  </tr>
  <tr>
  <% @books.each do |book| %>
    <td></td>
    <td><%= book.title %></td>
    <td><% book.body %></td>
    <td></td>
  <% end %>
  </tr>
</div>