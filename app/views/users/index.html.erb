<h1>index</h1>
<%= render 'partial/userinfo', book: @book %>

<div>
  <tr>
    <th>image</th>
    <th>name</th>
    <th></th>
  </tr>
  <tr>
  <% @users.each do |user| %>
    <th><%= attachment_image_tag user , :profile_image, size: "40x40" %></th>
    <th><%= user.name %></th>
    <th><%= link_to "Show", user_path(user.id) %></th>
  <% end %>
  </tr>
</div>